how to run:

use shell/command prompt and navigate into the folder jquery-example
run jqueryexample.py ($ python jqueryexample.py)
a url will print, navigate to it using a web browser
    for instance if the url is 127.0.0.1:5000, type that into chrome
the page should load 8-)